import { getContentProvider } from "@/content/providers";
import type { ListOptions } from "@/content/repositories";
import type { LegalDocument } from "@/content/types";
export const getSiteSettings = async () => (await getContentProvider()).getSiteSettings(); export const getNavigation = async () => (await getContentProvider()).getNavigation(); export const getHomePage = async () => (await getContentProvider()).getHomePage();
export const getDramas = async (options?: ListOptions) => (await getContentProvider()).getDramas(options); export const getFeaturedDramas = async () => (await getContentProvider()).getFeaturedDramas(); export const getDrama = async (slug: string) => (await getContentProvider()).getDramaBySlug(slug); export const getDramaCategories = async () => (await getContentProvider()).getDramaCategories();
export const getCreators = async () => (await getContentProvider()).getCreators(); export const getFeaturedCreators = async () => (await getContentProvider()).getFeaturedCreators(); export const getCreator = async (slug: string) => (await getContentProvider()).getCreatorBySlug(slug);
export const getArticles = async () => (await getContentProvider()).getArticles(); export const getFeaturedArticles = async () => (await getContentProvider()).getFeaturedArticles(); export const getArticle = async (slug: string) => (await getContentProvider()).getArticleBySlug(slug); export const getArticleCategories = async () => (await getContentProvider()).getArticleCategories();
export const getLegalDocument = async (slug: LegalDocument["slug"]) => (await getContentProvider()).getLegalPage(slug); export const getBusinessPage = async () => (await getContentProvider()).getBusinessPage(); export const getDownloadSettings = async () => (await getContentProvider()).getDownloadSettings();
